// nel3ab-game — application shell only (REQ-1.5).
// The Fastify server, /negotiate, the Web PubSub handler and the room registry
// are owned by Phase 11; containerisation and /healthz by Phase 9. No server
// process, no dependency on Fastify, and no timer exists in this phase.
export const PLACEHOLDER = true
