// @nel3ab/game — package shell only (REQ-1.5).
// The rules engine (RoomState, the reducer, the clock, round/match flow) is
// owned by Phases 3–4 and is deliberately absent here. See
// specs/phase-1/requirements.md §4.
export const PLACEHOLDER = true
