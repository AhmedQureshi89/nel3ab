node_modules/
.next/
dist/
*.tsbuildinfo

# Generated by `next build`, not authored. It is CRLF and it `/// <reference
# path="./.next/types/routes.d.ts" />`s a file that only exists after a build,
# so committing it would make `pnpm typecheck` fail on a fresh clone.
next-env.d.ts
.env
.env.local
*.log
